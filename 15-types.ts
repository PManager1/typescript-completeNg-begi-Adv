let a: number;
let b: boolean;

let c: string;

let e: any[] = [1,2,3];


// Enums allow us to define a set of named constants. 
//Using enums can make it easier to document intent, 
// or create a set of distinct cases. TypeScript provides both numeric and string-based enums.

let arr:any[] = [12,3,45,2,33,566,3]; 


const colorRed = 0;
const colorGreen = 0;
const colorBlue = 0;


enum Color { Red = 0, Green = 1, Purple=3 };

let backgroundColor = Color.

console.log( '16 -  backgroundColor = ', backgroundColor);